# Cursor Origin — client_manager

Origin: `amalan/FinanceFugue`
URL: `https://origin.cursor.com/amalan/FinanceFugue.git`
Ветка: `main`
GitHub: `kir-spec/FinanceFugue` (remote `origin`)

> Локальная папка `client_manager`, на Origin — `FinanceFugue`.

## Залить на Origin

```powershell
wsl -d Ubuntu -u composer -- git -C "/mnt/e/coding/client_manager" push cursor main
```

```bash
cd "/mnt/e/coding/client_manager"
git push origin main
git push cursor main
```

Remote (один раз): `git remote add cursor https://origin.cursor.com/amalan/FinanceFugue.git`

Полная инструкция: `E:\coding\CURSOR_ORIGIN.md`
